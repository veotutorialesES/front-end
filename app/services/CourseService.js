angular.module("app.course", ['app.api']).service("$course", function($api,$rootScope,$http,$window){
   // var self = this;





    this.course = {
        modules: [],
        find: function(id){

        }

    }

    // course.find(ID)




});